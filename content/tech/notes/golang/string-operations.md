Split string using `strings.Split` (`func Split(s, sep string) []string`)
```go
import strings

fmt.Println(strings.Split("hello world", " ")) //["hello", "world"]
```
[Split Documentation](https://pkg.go.dev/strings#Split)
